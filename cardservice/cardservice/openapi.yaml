# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: Card API
    version: 0.0.1
paths:
    /v1/helloworld/card:
        post:
            tags:
                - Card
            operationId: Card_CreateCard
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/CreateReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /v1/helloworld/card/{cardId}:
        get:
            tags:
                - Card
            operationId: Card_DisplayCard
            parameters:
                - name: cardId
                  in: path
                  required: true
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/FindByIdReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
        delete:
            tags:
                - Card
            operationId: Card_DeleteCard
            parameters:
                - name: cardId
                  in: path
                  required: true
                  schema:
                    type: string
                - name: accountId
                  in: query
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DeleteCardReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
        patch:
            tags:
                - Card
            operationId: Card_UpdateCard
            parameters:
                - name: cardId
                  in: path
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateCardRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/UpdateCardReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        CreateReply:
            type: object
            properties:
                message:
                    type: string
                cardId:
                    type: string
                accountId:
                    type: string
                cardnumber:
                    type: string
                cardtype:
                    type: string
                expirydate:
                    type: string
                dailylimit:
                    type: string
                monthlylimit:
                    type: string
                pinattempts:
                    type: string
        CreateRequest:
            type: object
            properties:
                cardId:
                    type: string
                accountId:
                    type: string
                cardnumber:
                    type: string
                cardtype:
                    type: string
                expirydate:
                    type: string
                dailylimit:
                    type: string
                monthlylimit:
                    type: string
                pinattempts:
                    type: string
        DeleteCardReply:
            type: object
            properties:
                message:
                    type: string
        FindByIdReply:
            type: object
            properties:
                display:
                    type: array
                    items:
                        $ref: '#/components/schemas/CreateReply'
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
        UpdateCardReply:
            type: object
            properties:
                message:
                    type: string
                display:
                    type: array
                    items:
                        $ref: '#/components/schemas/CreateReply'
        UpdateCardRequest:
            type: object
            properties:
                cardId:
                    type: string
                accountId:
                    type: string
                cardnumber:
                    type: string
                cardtype:
                    type: string
                dailylimit:
                    type: string
                monthlylimit:
                    type: string
tags:
    - name: Card
